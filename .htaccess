<IfModule mod_rewrite.c>
    RewriteEngine On

    # If request is for an existing file or folder, serve it
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Otherwise, send to index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>
